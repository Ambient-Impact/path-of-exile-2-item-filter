# ------------------------------------------------------------------------------
#   A custom Path of Exile 2 item filter for cool and attractive people.
# ------------------------------------------------------------------------------

{# Note that the order of these imports is significant. -#}
{% for group in [
  'defaults', 'uniques', 'quest_items', 'gems', 'salvage_materials',
  'jewellery', 'leagues', 'endgame_items', 'currency', 'socketables', 'jewels',
  'ascendency_items', 'belt_items',
] %}{% include "groups/" + group + "." + itemFilter.templateExtension with context %}
{% endfor -%}
